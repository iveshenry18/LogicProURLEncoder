# Logic Pro URL Encoder

This is a simple CLI tool that encodes the path to a `.logicx` file to a URL Deep Link that opens the given path in Logic Pro.

It also includes an equivalent MacOS Automator workflow.